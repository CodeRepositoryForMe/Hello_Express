<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Add Product Pug Page
        link(rel="stylesheet", href="/css/main.css")
        link(rel="stylesheet", href="/css/product.css")
    body
        header.main-header
            nav.mail-header_nav
                ul.main-header__item-list
                    li.main-header__item
                        a(href="/") Shop
                    li.main-header__item
                        a.active(href="/addproduct") Add Product \
        main.main
            form.product-form(action="/product" method="POST")
                .form-control
                    label(for="Title") Title
                    Input(type="text", name="Title")
                .form-control
                    label(for="Cost") Cost
                    Input(type="text", name="Cost")
                .form-control
                    label(for="Description") Description
                    Input(type="text", name="Description")
                .form-control
                    label(for="ProductLink") Product Link
                    Input(type="url", name="ProductLink")
                button.button(type="submit") Add Product
        