<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <link rel="stylesheet" href="/css/main.css">
    {{#if productCSS}}
        <link rel="stylesheet" href="/css/product.css">
    {{/if}}
</head>
<body>
    <header class="main-header">
        <nav class = "mail-header_nav">
            <ul class="main-header__item-list">
                <li class="main-header__item"><a class="{{#if catelog}} active {{/if}}" href="/catelog">Product Catelog</a></li>
                <li class="main-header__item"><a class="{{#if addproduct}} active {{/if}}" href="/product">Add Product</a></li>
            </ul>
        </nav>
    </header>
    {{{ body }}}
</body>
</html>